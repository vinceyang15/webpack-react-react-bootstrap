plugins {
  id 'base'
  id 'net.researchgate.release' version '2.4.0'
}

description = 'A trivial Gradle build'
version = '1.0'


task copy(type: Copy) {
  from 'app'
  into 'dest'
}

task zip(type: Zip) {
  from 'dest'
}

